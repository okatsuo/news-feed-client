import { createTheme } from '@material-ui/core';

const theme = createTheme({
  palette: {
    primary: {
      main: '#22272E',
    },
    secondary: {
      main: '#2D333B',
    },
  },
});

export default theme;