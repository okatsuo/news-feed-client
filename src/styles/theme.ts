import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3AADA3',
      contrastText: "#FFFFFF"
    },
    secondary: {
      main: '#FFFFFF',
    },
  },
});

export default theme;